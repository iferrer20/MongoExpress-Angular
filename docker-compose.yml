version: "3"  
services:
  web:
    image: nginx:latest
    container_name: "land_web"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend-angular/dist:/usr/share/nginx/html:ro
      - ./config/nginx.conf:/etc/nginx/conf.d/default.conf:ro
        #- ./data/img/:/usr/share/nginx/html/img/:rw
  db:
    image: mongo:4.4
    command: --quiet --logpath /dev/null
    container_name: "land_db"
      #volumes:
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=land
  backend:
    image: node:14
    container_name: "land_backend"
    working_dir: /home/node/app
    volumes: 
      - ./backend-express:/home/node/app
      - ./data/img/:/home/node/app/img
    command: /bin/bash -c "usermod -u `stat -c '%u' package.json` node && groupmod -g `stat -c '%g' package.json` node && su node -c 'npm install && npm run dev'"
  dev:
    image: node:14
    container_name: "land_dev"
    working_dir: /home/node/app
    volumes:
      - ./frontend-angular:/home/node/app
    command: /bin/bash -c "usermod -u `stat -c '%u' .` node && groupmod -g `stat -c '%g' .` node && su node -c 'npm install && npm start -- --port 80 --host 0.0.0.0 --disable-host-check --deploy-url /dev/ --base-href /dev/'"
